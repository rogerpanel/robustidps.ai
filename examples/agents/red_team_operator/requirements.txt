# Framework deps the starter needs.
langgraph>=0.2
httpx>=0.27

# The robustidps SDK is in this monorepo at ../../sdk — install it
# editable so changes here flow through without a republish:
#
#   pip install -e ../../sdk[aegis]
#
# When the SDK is published to PyPI, replace the line above with:
#   robustidps[aegis]>=0.4
