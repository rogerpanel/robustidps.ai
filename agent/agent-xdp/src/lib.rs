//! Placeholder library root for the RobustIDPS.ai XDP fast-path crate.
//!
//! The real userspace loader (XdpAgent, XdpStats, XdpConfig, AttachMode,
//! AttachedInfo) lands in the follow-up commit alongside `build.rs` and the
//! README. This stub exists only so the scaffolding commit (workspace
//! member + Cargo.toml + the kernel-side BPF C file) is coherent on its
//! own and the workspace continues to build.

#![warn(rust_2018_idioms)]
